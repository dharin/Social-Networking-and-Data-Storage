- if favoritable.favorited_by(current_user)
  = link_to_remote(:un_favorite_this.l, :url => {:action => "toggle_favorite", :id => @upload.id}, :method => :get)
- else
  = link_to_remote(:favorite_this.l, :url => {:action => "toggle_favorite", :id => @upload.id}, :method => :get)